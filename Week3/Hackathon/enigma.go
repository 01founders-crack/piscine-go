package piscinego

func Enigma(a ***int, b *int, c *******int, d ****int) {
	tempa := ***a
	tempb := *b
	tempc := *******c
	tempd := ****d

	***a = tempb
	*b = tempd
	*******c = tempa
	****d = tempc
}
